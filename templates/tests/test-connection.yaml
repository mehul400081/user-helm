apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "user-helm.fullname" . }}-test-connection"
  labels:
    {{- include "user-helm.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "user-helm.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
